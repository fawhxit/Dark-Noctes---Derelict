/// @description Logic

#region Yield Action (Disabled)
	
	/*
	if(parent != noone and parent.active) {
		
		// Mouse Inside Logic
		if(ds_list_size(M.m.inst_l) > 0) {
				
			// Take/Yield/Mine? DISABLED
			if(yield and M.m.inst_l[|0] == id and mbl) {
				
				if(yield_deli >= yield_del) {
					yield_do = t
					ds_list_clear(M.m.inst_l)
					instance_destroy(self)
				} else yield_deli += 1
				
			} else if(yield) yield_deli = 0
			
		}
		
	}
	*/
	
#endregion